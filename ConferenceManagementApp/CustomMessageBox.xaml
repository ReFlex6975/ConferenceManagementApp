<Window x:Class="ConferenceManagementApp.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConferenceManagementApp"
        mc:Ignorable="d"
        Title="Message" Height="200" Width="400" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Developer Gleb Fedorov" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="0" Margin="0,37,0,0"/>
                <TextBlock Text="2024" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="0" Margin="0,71,0,0"/>

                <Button Content="OK" Width="80" Height="30" Margin="0,10" Click="OkButton_Click" Grid.Row="1" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
